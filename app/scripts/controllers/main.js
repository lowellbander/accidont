'use strict';

/**
 * @ngdoc function
 * @name accidontApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the accidontApp
 */
angular.module('accidontApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
