'use strict';

/**
 * @ngdoc function
 * @name accidontApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the accidontApp
 */
angular.module('accidontApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
